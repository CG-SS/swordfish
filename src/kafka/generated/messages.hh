// Generated by tools/gen_kafka_protocol.py from the official Kafka
// protocol schemas in third_party/kafka-protocol. Do not edit.
#pragma once
#include "swordfish/kafka/wire.hh"
#include <optional>
#include <string>
#include <vector>

namespace sf::kafka {

struct api_versions_response_api_version {
    int16_t                                  api_key = 0;
    int16_t                                  min_version = 0;
    int16_t                                  max_version = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct api_versions_response_supported_feature_key {
    std::string                              name;
    int16_t                                  min_version = 0;
    int16_t                                  max_version = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct api_versions_response_finalized_feature_key {
    std::string                              name;
    int16_t                                  max_version_level = 0;
    int16_t                                  min_version_level = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct consumer_protocol_assignment_topic_partition {
    std::string                              topic;
    std::vector<int32_t>                     partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct consumer_protocol_subscription_topic_partition {
    std::string                              topic;
    std::vector<int32_t>                     partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct describe_groups_response_described_group_member {
    std::string                              member_id;
    std::optional<std::string>               group_instance_id;
    std::string                              client_id;
    std::string                              client_host;
    std::string                              member_metadata;
    std::string                              member_assignment;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct describe_groups_response_described_group {
    int16_t                                  error_code = 0;
    std::optional<std::string>               error_message;
    std::string                              group_id;
    std::string                              group_state;
    std::string                              protocol_type;
    std::string                              protocol_data;
    std::vector<describe_groups_response_described_group_member> members;
    int32_t                                  authorized_operations = -2147483648;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_request_replica_state {
    int32_t                                  replica_id = -1;
    int64_t                                  replica_epoch = -1LL;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_request_fetch_partition {
    int32_t                                  partition = 0;
    int32_t                                  current_leader_epoch = -1;
    int64_t                                  fetch_offset = 0;
    int32_t                                  last_fetched_epoch = -1;
    int64_t                                  log_start_offset = -1LL;
    int32_t                                  partition_max_bytes = 0;
    uuid                                     replica_directory_id;
    int64_t                                  high_watermark = 9223372036854775807LL;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_request_fetch_topic {
    std::string                              topic;
    uuid                                     topic_id;
    std::vector<fetch_request_fetch_partition> partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_request_forgotten_topic {
    std::string                              topic;
    uuid                                     topic_id;
    std::vector<int32_t>                     partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_response_epoch_end_offset {
    int32_t                                  epoch = -1;
    int64_t                                  end_offset = -1LL;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_response_leader_id_and_epoch {
    int32_t                                  leader_id = -1;
    int32_t                                  leader_epoch = -1;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_response_snapshot_id {
    int64_t                                  end_offset = -1LL;
    int32_t                                  epoch = -1;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_response_aborted_transaction {
    int64_t                                  producer_id = 0;
    int64_t                                  first_offset = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_response_partition_data {
    int32_t                                  partition_index = 0;
    int16_t                                  error_code = 0;
    int64_t                                  high_watermark = 0;
    int64_t                                  last_stable_offset = -1LL;
    int64_t                                  log_start_offset = -1LL;
    std::optional<fetch_response_epoch_end_offset> diverging_epoch;
    std::optional<fetch_response_leader_id_and_epoch> current_leader;
    std::optional<fetch_response_snapshot_id> snapshot_id;
    std::vector<fetch_response_aborted_transaction> aborted_transactions;
    int32_t                                  preferred_read_replica = -1;
    std::optional<std::string>               records;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_response_fetchable_topic_response {
    std::string                              topic;
    uuid                                     topic_id;
    std::vector<fetch_response_partition_data> partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct fetch_response_node_endpoint {
    int32_t                                  node_id = 0;
    std::string                              host;
    int32_t                                  port = 0;
    std::optional<std::string>               rack;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct find_coordinator_response_coordinator {
    std::string                              key;
    int32_t                                  node_id = 0;
    std::string                              host;
    int32_t                                  port = 0;
    int16_t                                  error_code = 0;
    std::optional<std::string>               error_message;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct join_group_request_protocol {
    std::string                              name;
    std::string                              metadata;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct join_group_response_member {
    std::string                              member_id;
    std::optional<std::string>               group_instance_id;
    std::string                              metadata;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct leave_group_request_member_identity {
    std::string                              member_id;
    std::optional<std::string>               group_instance_id;
    std::optional<std::string>               reason;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct leave_group_response_member_response {
    std::string                              member_id;
    std::optional<std::string>               group_instance_id;
    int16_t                                  error_code = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct list_offsets_request_list_offsets_partition {
    int32_t                                  partition_index = 0;
    int32_t                                  current_leader_epoch = -1;
    int64_t                                  timestamp = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct list_offsets_request_list_offsets_topic {
    std::string                              name;
    std::vector<list_offsets_request_list_offsets_partition> partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct list_offsets_response_list_offsets_partition_response {
    int32_t                                  partition_index = 0;
    int16_t                                  error_code = 0;
    int64_t                                  timestamp = -1LL;
    int64_t                                  offset = -1LL;
    int32_t                                  leader_epoch = -1;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct list_offsets_response_list_offsets_topic_response {
    std::string                              name;
    std::vector<list_offsets_response_list_offsets_partition_response> partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct metadata_request_topic {
    uuid                                     topic_id;
    std::optional<std::string>               name;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct metadata_response_broker {
    int32_t                                  node_id = 0;
    std::string                              host;
    int32_t                                  port = 0;
    std::optional<std::string>               rack;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct metadata_response_partition {
    int16_t                                  error_code = 0;
    int32_t                                  partition_index = 0;
    int32_t                                  leader_id = 0;
    int32_t                                  leader_epoch = -1;
    std::vector<int32_t>                     replica_nodes;
    std::vector<int32_t>                     isr_nodes;
    std::vector<int32_t>                     offline_replicas;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct metadata_response_topic {
    int16_t                                  error_code = 0;
    std::optional<std::string>               name;
    uuid                                     topic_id;
    bool                                     is_internal = false;
    std::vector<metadata_response_partition> partitions;
    int32_t                                  topic_authorized_operations = -2147483648;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_commit_request_partition {
    int32_t                                  partition_index = 0;
    int64_t                                  committed_offset = 0;
    int32_t                                  committed_leader_epoch = -1;
    std::optional<std::string>               committed_metadata;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_commit_request_topic {
    std::string                              name;
    uuid                                     topic_id;
    std::vector<offset_commit_request_partition> partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_commit_response_partition {
    int32_t                                  partition_index = 0;
    int16_t                                  error_code = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_commit_response_topic {
    std::string                              name;
    uuid                                     topic_id;
    std::vector<offset_commit_response_partition> partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_fetch_request_topic {
    std::string                              name;
    std::vector<int32_t>                     partition_indexes;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_fetch_request_topics {
    std::string                              name;
    uuid                                     topic_id;
    std::vector<int32_t>                     partition_indexes;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_fetch_request_group {
    std::string                              group_id;
    std::optional<std::string>               member_id;
    int32_t                                  member_epoch = -1;
    std::vector<offset_fetch_request_topics> topics;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_fetch_response_partition {
    int32_t                                  partition_index = 0;
    int64_t                                  committed_offset = 0;
    int32_t                                  committed_leader_epoch = -1;
    std::optional<std::string>               metadata;
    int16_t                                  error_code = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_fetch_response_topic {
    std::string                              name;
    std::vector<offset_fetch_response_partition> partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_fetch_response_partitions {
    int32_t                                  partition_index = 0;
    int64_t                                  committed_offset = 0;
    int32_t                                  committed_leader_epoch = -1;
    std::optional<std::string>               metadata;
    int16_t                                  error_code = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_fetch_response_topics {
    std::string                              name;
    uuid                                     topic_id;
    std::vector<offset_fetch_response_partitions> partitions;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct offset_fetch_response_group {
    std::string                              group_id;
    std::vector<offset_fetch_response_topics> topics;
    int16_t                                  error_code = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct produce_request_partition_produce_data {
    int32_t                                  index = 0;
    std::optional<std::string>               records;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct produce_request_topic_produce_data {
    std::string                              name;
    uuid                                     topic_id;
    std::vector<produce_request_partition_produce_data> partition_data;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct produce_response_batch_index_and_error_message {
    int32_t                                  batch_index = 0;
    std::optional<std::string>               batch_index_error_message;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct produce_response_leader_id_and_epoch {
    int32_t                                  leader_id = -1;
    int32_t                                  leader_epoch = -1;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct produce_response_partition_produce_response {
    int32_t                                  index = 0;
    int16_t                                  error_code = 0;
    int64_t                                  base_offset = 0;
    int64_t                                  log_append_time_ms = -1LL;
    int64_t                                  log_start_offset = -1LL;
    std::vector<produce_response_batch_index_and_error_message> record_errors;
    std::optional<std::string>               error_message;
    std::optional<produce_response_leader_id_and_epoch> current_leader;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct produce_response_topic_produce_response {
    std::string                              name;
    uuid                                     topic_id;
    std::vector<produce_response_partition_produce_response> partition_responses;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct produce_response_node_endpoint {
    int32_t                                  node_id = 0;
    std::string                              host;
    int32_t                                  port = 0;
    std::optional<std::string>               rack;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct sync_group_request_assignment {
    std::string                              member_id;
    std::string                              assignment;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
};

struct api_versions_request {
    static constexpr int16_t api_key = 18;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 4;
    static constexpr int16_t flexible_from = 3;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              client_software_name;
    std::string                              client_software_version;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct api_versions_response {
    static constexpr int16_t api_key = 18;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 4;
    static constexpr int16_t flexible_from = 3;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int16_t                                  error_code = 0;
    std::vector<api_versions_response_api_version> api_keys;
    int32_t                                  throttle_time_ms = 0;
    std::vector<api_versions_response_supported_feature_key> supported_features;
    int64_t                                  finalized_features_epoch = -1LL;
    std::vector<api_versions_response_finalized_feature_key> finalized_features;
    bool                                     zk_migration_ready = false;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct consumer_protocol_assignment {
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 3;
    static constexpr int16_t flexible_from = -1;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::vector<consumer_protocol_assignment_topic_partition> assigned_partitions;
    std::optional<std::string>               user_data;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct consumer_protocol_subscription {
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 3;
    static constexpr int16_t flexible_from = -1;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::vector<std::string>                 topics;
    std::optional<std::string>               user_data;
    std::vector<consumer_protocol_subscription_topic_partition> owned_partitions;
    int32_t                                  generation_id = -1;
    std::optional<std::string>               rack_id;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct describe_groups_request {
    static constexpr int16_t api_key = 15;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 6;
    static constexpr int16_t flexible_from = 5;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::vector<std::string>                 groups;
    bool                                     include_authorized_operations = false;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct describe_groups_response {
    static constexpr int16_t api_key = 15;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 6;
    static constexpr int16_t flexible_from = 5;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    std::vector<describe_groups_response_described_group> groups;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct fetch_request {
    static constexpr int16_t api_key = 1;
    static constexpr int16_t min_version = 4;
    static constexpr int16_t max_version = 18;
    static constexpr int16_t flexible_from = 12;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::optional<std::string>               cluster_id;
    int32_t                                  replica_id = -1;
    std::optional<fetch_request_replica_state> replica_state;
    int32_t                                  max_wait_ms = 0;
    int32_t                                  min_bytes = 0;
    int32_t                                  max_bytes = 0x7fffffff;
    int8_t                                   isolation_level = 0;
    int32_t                                  session_id = 0;
    int32_t                                  session_epoch = -1;
    std::vector<fetch_request_fetch_topic>   topics;
    std::vector<fetch_request_forgotten_topic> forgotten_topics_data;
    std::string                              rack_id = "";
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct fetch_response {
    static constexpr int16_t api_key = 1;
    static constexpr int16_t min_version = 4;
    static constexpr int16_t max_version = 18;
    static constexpr int16_t flexible_from = 12;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    int16_t                                  error_code = 0;
    int32_t                                  session_id = 0;
    std::vector<fetch_response_fetchable_topic_response> responses;
    std::vector<fetch_response_node_endpoint> node_endpoints;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct find_coordinator_request {
    static constexpr int16_t api_key = 10;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 6;
    static constexpr int16_t flexible_from = 3;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              key;
    int8_t                                   key_type = 0;
    std::vector<std::string>                 coordinator_keys;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct find_coordinator_response {
    static constexpr int16_t api_key = 10;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 6;
    static constexpr int16_t flexible_from = 3;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    int16_t                                  error_code = 0;
    std::optional<std::string>               error_message;
    int32_t                                  node_id = 0;
    std::string                              host;
    int32_t                                  port = 0;
    std::vector<find_coordinator_response_coordinator> coordinators;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct heartbeat_request {
    static constexpr int16_t api_key = 12;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 4;
    static constexpr int16_t flexible_from = 4;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              group_id;
    int32_t                                  generation_id = 0;
    std::string                              member_id;
    std::optional<std::string>               group_instance_id;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct heartbeat_response {
    static constexpr int16_t api_key = 12;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 4;
    static constexpr int16_t flexible_from = 4;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    int16_t                                  error_code = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct join_group_request {
    static constexpr int16_t api_key = 11;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 9;
    static constexpr int16_t flexible_from = 6;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              group_id;
    int32_t                                  session_timeout_ms = 0;
    int32_t                                  rebalance_timeout_ms = -1;
    std::string                              member_id;
    std::optional<std::string>               group_instance_id;
    std::string                              protocol_type;
    std::vector<join_group_request_protocol> protocols;
    std::optional<std::string>               reason;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct join_group_response {
    static constexpr int16_t api_key = 11;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 9;
    static constexpr int16_t flexible_from = 6;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    int16_t                                  error_code = 0;
    int32_t                                  generation_id = -1;
    std::optional<std::string>               protocol_type;
    std::optional<std::string>               protocol_name;
    std::string                              leader;
    bool                                     skip_assignment = false;
    std::string                              member_id;
    std::vector<join_group_response_member>  members;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct leave_group_request {
    static constexpr int16_t api_key = 13;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 5;
    static constexpr int16_t flexible_from = 4;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              group_id;
    std::string                              member_id;
    std::vector<leave_group_request_member_identity> members;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct leave_group_response {
    static constexpr int16_t api_key = 13;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 5;
    static constexpr int16_t flexible_from = 4;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    int16_t                                  error_code = 0;
    std::vector<leave_group_response_member_response> members;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct list_offsets_request {
    static constexpr int16_t api_key = 2;
    static constexpr int16_t min_version = 1;
    static constexpr int16_t max_version = 11;
    static constexpr int16_t flexible_from = 6;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  replica_id = 0;
    int8_t                                   isolation_level = 0;
    std::vector<list_offsets_request_list_offsets_topic> topics;
    int32_t                                  timeout_ms = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct list_offsets_response {
    static constexpr int16_t api_key = 2;
    static constexpr int16_t min_version = 1;
    static constexpr int16_t max_version = 11;
    static constexpr int16_t flexible_from = 6;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    std::vector<list_offsets_response_list_offsets_topic_response> topics;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct metadata_request {
    static constexpr int16_t api_key = 3;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 13;
    static constexpr int16_t flexible_from = 9;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::vector<metadata_request_topic>      topics;
    bool                                     allow_auto_topic_creation = true;
    bool                                     include_cluster_authorized_operations = false;
    bool                                     include_topic_authorized_operations = false;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct metadata_response {
    static constexpr int16_t api_key = 3;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 13;
    static constexpr int16_t flexible_from = 9;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    std::vector<metadata_response_broker>    brokers;
    std::optional<std::string>               cluster_id;
    int32_t                                  controller_id = -1;
    std::vector<metadata_response_topic>     topics;
    int32_t                                  cluster_authorized_operations = -2147483648;
    int16_t                                  error_code = 0;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct offset_commit_request {
    static constexpr int16_t api_key = 8;
    static constexpr int16_t min_version = 2;
    static constexpr int16_t max_version = 10;
    static constexpr int16_t flexible_from = 8;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              group_id;
    int32_t                                  generation_id_or_member_epoch = -1;
    std::string                              member_id;
    std::optional<std::string>               group_instance_id;
    int64_t                                  retention_time_ms = -1LL;
    std::vector<offset_commit_request_topic> topics;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct offset_commit_response {
    static constexpr int16_t api_key = 8;
    static constexpr int16_t min_version = 2;
    static constexpr int16_t max_version = 10;
    static constexpr int16_t flexible_from = 8;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    std::vector<offset_commit_response_topic> topics;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct offset_fetch_request {
    static constexpr int16_t api_key = 9;
    static constexpr int16_t min_version = 1;
    static constexpr int16_t max_version = 10;
    static constexpr int16_t flexible_from = 6;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              group_id;
    std::vector<offset_fetch_request_topic>  topics;
    std::vector<offset_fetch_request_group>  groups;
    bool                                     require_stable = false;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct offset_fetch_response {
    static constexpr int16_t api_key = 9;
    static constexpr int16_t min_version = 1;
    static constexpr int16_t max_version = 10;
    static constexpr int16_t flexible_from = 6;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    std::vector<offset_fetch_response_topic> topics;
    int16_t                                  error_code = 0;
    std::vector<offset_fetch_response_group> groups;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct produce_request {
    static constexpr int16_t api_key = 0;
    static constexpr int16_t min_version = 3;
    static constexpr int16_t max_version = 13;
    static constexpr int16_t flexible_from = 9;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::optional<std::string>               transactional_id;
    int16_t                                  acks = 0;
    int32_t                                  timeout_ms = 0;
    std::vector<produce_request_topic_produce_data> topic_data;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct produce_response {
    static constexpr int16_t api_key = 0;
    static constexpr int16_t min_version = 3;
    static constexpr int16_t max_version = 13;
    static constexpr int16_t flexible_from = 9;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::vector<produce_response_topic_produce_response> responses;
    int32_t                                  throttle_time_ms = 0;
    std::vector<produce_response_node_endpoint> node_endpoints;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct sasl_authenticate_request {
    static constexpr int16_t api_key = 36;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 2;
    static constexpr int16_t flexible_from = 2;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              auth_bytes;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct sasl_authenticate_response {
    static constexpr int16_t api_key = 36;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 2;
    static constexpr int16_t flexible_from = 2;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int16_t                                  error_code = 0;
    std::optional<std::string>               error_message;
    std::string                              auth_bytes;
    int64_t                                  session_lifetime_ms = 0LL;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct sasl_handshake_request {
    static constexpr int16_t api_key = 17;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 1;
    static constexpr int16_t flexible_from = -1;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              mechanism;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct sasl_handshake_response {
    static constexpr int16_t api_key = 17;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 1;
    static constexpr int16_t flexible_from = -1;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int16_t                                  error_code = 0;
    std::vector<std::string>                 mechanisms;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct sync_group_request {
    static constexpr int16_t api_key = 14;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 5;
    static constexpr int16_t flexible_from = 4;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    std::string                              group_id;
    int32_t                                  generation_id = 0;
    std::string                              member_id;
    std::optional<std::string>               group_instance_id;
    std::optional<std::string>               protocol_type;
    std::optional<std::string>               protocol_name;
    std::vector<sync_group_request_assignment> assignments;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

struct sync_group_response {
    static constexpr int16_t api_key = 14;
    static constexpr int16_t min_version = 0;
    static constexpr int16_t max_version = 5;
    static constexpr int16_t flexible_from = 4;
    static constexpr bool flexible(int16_t v) {
        return flexible_from >= 0 && v >= flexible_from;
    }
    int32_t                                  throttle_time_ms = 0;
    int16_t                                  error_code = 0;
    std::optional<std::string>               protocol_type;
    std::optional<std::string>               protocol_name;
    std::string                              assignment;
    void encode(writer& w, int16_t version, bool flex) const;
    void decode(reader& r, int16_t version, bool flex);
    void encode(writer& w, int16_t version) const { encode(w, version, flexible(version)); }
    void decode(reader& r, int16_t version) { decode(r, version, flexible(version)); }
};

} // namespace sf::kafka
