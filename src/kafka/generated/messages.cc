// Generated by tools/gen_kafka_protocol.py. Do not edit.
#include "messages.hh"

namespace sf::kafka {

void api_versions_response_api_version::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i16(api_key);
        w.i16(min_version);
        w.i16(max_version);
    }
    if (flex) {
        w.empty_tags();
    }
}

void api_versions_response_api_version::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        api_key = r.i16();
        min_version = r.i16();
        max_version = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void api_versions_response_supported_feature_key::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.string(name, flex);
        w.i16(min_version);
        w.i16(max_version);
    }
    if (flex) {
        w.empty_tags();
    }
}

void api_versions_response_supported_feature_key::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        name = r.string(flex);
        min_version = r.i16();
        max_version = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void api_versions_response_finalized_feature_key::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.string(name, flex);
        w.i16(max_version_level);
        w.i16(min_version_level);
    }
    if (flex) {
        w.empty_tags();
    }
}

void api_versions_response_finalized_feature_key::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        name = r.string(flex);
        max_version_level = r.i16();
        min_version_level = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void consumer_protocol_assignment_topic_partition::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(topic, flex);
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            w.i32(e_);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void consumer_protocol_assignment_topic_partition::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        topic = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_ = r.i32();
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void consumer_protocol_subscription_topic_partition::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 1) {
        w.string(topic, flex);
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            w.i32(e_);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void consumer_protocol_subscription_topic_partition::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 1) {
        topic = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_ = r.i32();
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void describe_groups_response_described_group_member::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(member_id, flex);
    }
    if (version >= 4) {
        w.nullable_string(group_instance_id, flex);
    }
    if (version >= 0) {
        w.string(client_id, flex);
        w.string(client_host, flex);
        w.bytes(member_metadata, flex);
        w.bytes(member_assignment, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void describe_groups_response_described_group_member::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        member_id = r.string(flex);
    }
    if (version >= 4) {
        group_instance_id = r.nullable_string(flex);
    }
    if (version >= 0) {
        client_id = r.string(flex);
        client_host = r.string(flex);
        member_metadata = r.bytes(flex);
        member_assignment = r.bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void describe_groups_response_described_group::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i16(error_code);
    }
    if (version >= 6) {
        w.nullable_string(error_message, flex);
    }
    if (version >= 0) {
        w.string(group_id, flex);
        w.string(group_state, flex);
        w.string(protocol_type, flex);
        w.string(protocol_data, flex);
        w.array_len(members.size(), flex);
        for (const auto& e_ : members) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 3) {
        w.i32(authorized_operations);
    }
    if (flex) {
        w.empty_tags();
    }
}

void describe_groups_response_described_group::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        error_code = r.i16();
    }
    if (version >= 6) {
        error_message = r.nullable_string(flex);
    }
    if (version >= 0) {
        group_id = r.string(flex);
        group_state = r.string(flex);
        protocol_type = r.string(flex);
        protocol_data = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            members.clear();
            if (n_ > 0) {
                members.resize(static_cast<size_t>(n_));
                for (auto& e_ : members) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 3) {
        authorized_operations = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_request_replica_state::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 15) {
        w.i32(replica_id);
        w.i64(replica_epoch);
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_request_replica_state::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 15) {
        replica_id = r.i32();
        replica_epoch = r.i64();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_request_fetch_partition::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(partition);
    }
    if (version >= 9) {
        w.i32(current_leader_epoch);
    }
    if (version >= 0) {
        w.i64(fetch_offset);
    }
    if (version >= 12) {
        w.i32(last_fetched_epoch);
    }
    if (version >= 5) {
        w.i64(log_start_offset);
    }
    if (version >= 0) {
        w.i32(partition_max_bytes);
    }
    if (flex) {
        writer t_;
        uint32_t n_ = 0;
        if (version >= 17 && !replica_directory_id.is_zero()) {
            writer b_;
            b_.uuid_(replica_directory_id);
            t_.uvarint(0);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        if (version >= 18 && high_watermark != 9223372036854775807LL) {
            writer b_;
            b_.i64(high_watermark);
            t_.uvarint(1);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        w.uvarint(n_);
        w.raw(t_.view());
    }
}

void fetch_request_fetch_partition::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        partition = r.i32();
    }
    if (version >= 9) {
        current_leader_epoch = r.i32();
    }
    if (version >= 0) {
        fetch_offset = r.i64();
    }
    if (version >= 12) {
        last_fetched_epoch = r.i32();
    }
    if (version >= 5) {
        log_start_offset = r.i64();
    }
    if (version >= 0) {
        partition_max_bytes = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            case 0:
                if (version >= 17) {
                    replica_directory_id = r.uuid_();
                }
                break;
            case 1:
                if (version >= 18) {
                    high_watermark = r.i64();
                }
                break;
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_request_fetch_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 12) {
        w.string(topic, flex);
    }
    if (version >= 13) {
        w.uuid_(topic_id);
    }
    if (version >= 0) {
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_request_fetch_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 12) {
        topic = r.string(flex);
    }
    if (version >= 13) {
        topic_id = r.uuid_();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_request_forgotten_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 7 && version <= 12) {
        w.string(topic, flex);
    }
    if (version >= 13) {
        w.uuid_(topic_id);
    }
    if (version >= 7) {
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            w.i32(e_);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_request_forgotten_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 7 && version <= 12) {
        topic = r.string(flex);
    }
    if (version >= 13) {
        topic_id = r.uuid_();
    }
    if (version >= 7) {
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_ = r.i32();
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_response_epoch_end_offset::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 12) {
        w.i32(epoch);
        w.i64(end_offset);
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_response_epoch_end_offset::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 12) {
        epoch = r.i32();
        end_offset = r.i64();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_response_leader_id_and_epoch::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 12) {
        w.i32(leader_id);
        w.i32(leader_epoch);
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_response_leader_id_and_epoch::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 12) {
        leader_id = r.i32();
        leader_epoch = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_response_snapshot_id::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i64(end_offset);
        w.i32(epoch);
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_response_snapshot_id::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        end_offset = r.i64();
        epoch = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_response_aborted_transaction::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 4) {
        w.i64(producer_id);
        w.i64(first_offset);
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_response_aborted_transaction::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 4) {
        producer_id = r.i64();
        first_offset = r.i64();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_response_partition_data::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(partition_index);
        w.i16(error_code);
        w.i64(high_watermark);
    }
    if (version >= 4) {
        w.i64(last_stable_offset);
    }
    if (version >= 5) {
        w.i64(log_start_offset);
    }
    if (version >= 4) {
        if (aborted_transactions.empty() && version >= 4) {
            w.null_array(flex);
        } else {
            w.array_len(aborted_transactions.size(), flex);
            for (const auto& e_ : aborted_transactions) {
                e_.encode(w, version, flex);
            }
        }
    }
    if (version >= 11) {
        w.i32(preferred_read_replica);
    }
    if (version >= 0) {
        w.nullable_bytes(records, flex);
    }
    if (flex) {
        writer t_;
        uint32_t n_ = 0;
        if (version >= 12 && diverging_epoch.has_value()) {
            writer b_;
            diverging_epoch->encode(b_, version, flex);
            t_.uvarint(0);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        if (version >= 12 && current_leader.has_value()) {
            writer b_;
            current_leader->encode(b_, version, flex);
            t_.uvarint(1);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        if (version >= 12 && snapshot_id.has_value()) {
            writer b_;
            snapshot_id->encode(b_, version, flex);
            t_.uvarint(2);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        w.uvarint(n_);
        w.raw(t_.view());
    }
}

void fetch_response_partition_data::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        partition_index = r.i32();
        error_code = r.i16();
        high_watermark = r.i64();
    }
    if (version >= 4) {
        last_stable_offset = r.i64();
    }
    if (version >= 5) {
        log_start_offset = r.i64();
    }
    if (version >= 4) {
        {
            const int64_t n_ = r.array_len(flex);
            aborted_transactions.clear();
            if (n_ > 0) {
                aborted_transactions.resize(static_cast<size_t>(n_));
                for (auto& e_ : aborted_transactions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 11) {
        preferred_read_replica = r.i32();
    }
    if (version >= 0) {
        records = r.nullable_bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            case 0:
                if (version >= 12) {
                    diverging_epoch.emplace();
                    diverging_epoch->decode(r, version, flex);
                }
                break;
            case 1:
                if (version >= 12) {
                    current_leader.emplace();
                    current_leader->decode(r, version, flex);
                }
                break;
            case 2:
                if (version >= 12) {
                    snapshot_id.emplace();
                    snapshot_id->decode(r, version, flex);
                }
                break;
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_response_fetchable_topic_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 12) {
        w.string(topic, flex);
    }
    if (version >= 13) {
        w.uuid_(topic_id);
    }
    if (version >= 0) {
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_response_fetchable_topic_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 12) {
        topic = r.string(flex);
    }
    if (version >= 13) {
        topic_id = r.uuid_();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_response_node_endpoint::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 16) {
        w.i32(node_id);
        w.string(host, flex);
        w.i32(port);
        w.nullable_string(rack, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void fetch_response_node_endpoint::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 16) {
        node_id = r.i32();
        host = r.string(flex);
        port = r.i32();
        rack = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void find_coordinator_response_coordinator::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 4) {
        w.string(key, flex);
        w.i32(node_id);
        w.string(host, flex);
        w.i32(port);
        w.i16(error_code);
        w.nullable_string(error_message, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void find_coordinator_response_coordinator::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 4) {
        key = r.string(flex);
        node_id = r.i32();
        host = r.string(flex);
        port = r.i32();
        error_code = r.i16();
        error_message = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void join_group_request_protocol::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(name, flex);
        w.bytes(metadata, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void join_group_request_protocol::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        name = r.string(flex);
        metadata = r.bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void join_group_response_member::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(member_id, flex);
    }
    if (version >= 5) {
        w.nullable_string(group_instance_id, flex);
    }
    if (version >= 0) {
        w.bytes(metadata, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void join_group_response_member::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        member_id = r.string(flex);
    }
    if (version >= 5) {
        group_instance_id = r.nullable_string(flex);
    }
    if (version >= 0) {
        metadata = r.bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void leave_group_request_member_identity::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.string(member_id, flex);
        w.nullable_string(group_instance_id, flex);
    }
    if (version >= 5) {
        w.nullable_string(reason, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void leave_group_request_member_identity::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        member_id = r.string(flex);
        group_instance_id = r.nullable_string(flex);
    }
    if (version >= 5) {
        reason = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void leave_group_response_member_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.string(member_id, flex);
        w.nullable_string(group_instance_id, flex);
        w.i16(error_code);
    }
    if (flex) {
        w.empty_tags();
    }
}

void leave_group_response_member_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        member_id = r.string(flex);
        group_instance_id = r.nullable_string(flex);
        error_code = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void list_offsets_request_list_offsets_partition::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(partition_index);
    }
    if (version >= 4) {
        w.i32(current_leader_epoch);
    }
    if (version >= 0) {
        w.i64(timestamp);
    }
    if (flex) {
        w.empty_tags();
    }
}

void list_offsets_request_list_offsets_partition::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        partition_index = r.i32();
    }
    if (version >= 4) {
        current_leader_epoch = r.i32();
    }
    if (version >= 0) {
        timestamp = r.i64();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void list_offsets_request_list_offsets_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(name, flex);
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void list_offsets_request_list_offsets_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        name = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void list_offsets_response_list_offsets_partition_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(partition_index);
        w.i16(error_code);
    }
    if (version >= 1) {
        w.i64(timestamp);
        w.i64(offset);
    }
    if (version >= 4) {
        w.i32(leader_epoch);
    }
    if (flex) {
        w.empty_tags();
    }
}

void list_offsets_response_list_offsets_partition_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        partition_index = r.i32();
        error_code = r.i16();
    }
    if (version >= 1) {
        timestamp = r.i64();
        offset = r.i64();
    }
    if (version >= 4) {
        leader_epoch = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void list_offsets_response_list_offsets_topic_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(name, flex);
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void list_offsets_response_list_offsets_topic_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        name = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void metadata_request_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 10) {
        w.uuid_(topic_id);
    }
    if (version >= 0) {
        w.nullable_string(name, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void metadata_request_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 10) {
        topic_id = r.uuid_();
    }
    if (version >= 0) {
        name = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void metadata_response_broker::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(node_id);
        w.string(host, flex);
        w.i32(port);
    }
    if (version >= 1) {
        w.nullable_string(rack, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void metadata_response_broker::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        node_id = r.i32();
        host = r.string(flex);
        port = r.i32();
    }
    if (version >= 1) {
        rack = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void metadata_response_partition::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i16(error_code);
        w.i32(partition_index);
        w.i32(leader_id);
    }
    if (version >= 7) {
        w.i32(leader_epoch);
    }
    if (version >= 0) {
        w.array_len(replica_nodes.size(), flex);
        for (const auto& e_ : replica_nodes) {
            w.i32(e_);
        }
        w.array_len(isr_nodes.size(), flex);
        for (const auto& e_ : isr_nodes) {
            w.i32(e_);
        }
    }
    if (version >= 5) {
        w.array_len(offline_replicas.size(), flex);
        for (const auto& e_ : offline_replicas) {
            w.i32(e_);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void metadata_response_partition::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        error_code = r.i16();
        partition_index = r.i32();
        leader_id = r.i32();
    }
    if (version >= 7) {
        leader_epoch = r.i32();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            replica_nodes.clear();
            if (n_ > 0) {
                replica_nodes.resize(static_cast<size_t>(n_));
                for (auto& e_ : replica_nodes) {
                    e_ = r.i32();
                }
            }
        }
        {
            const int64_t n_ = r.array_len(flex);
            isr_nodes.clear();
            if (n_ > 0) {
                isr_nodes.resize(static_cast<size_t>(n_));
                for (auto& e_ : isr_nodes) {
                    e_ = r.i32();
                }
            }
        }
    }
    if (version >= 5) {
        {
            const int64_t n_ = r.array_len(flex);
            offline_replicas.clear();
            if (n_ > 0) {
                offline_replicas.resize(static_cast<size_t>(n_));
                for (auto& e_ : offline_replicas) {
                    e_ = r.i32();
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void metadata_response_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i16(error_code);
        w.nullable_string(name, flex);
    }
    if (version >= 10) {
        w.uuid_(topic_id);
    }
    if (version >= 1) {
        w.boolean(is_internal);
    }
    if (version >= 0) {
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 8) {
        w.i32(topic_authorized_operations);
    }
    if (flex) {
        w.empty_tags();
    }
}

void metadata_response_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        error_code = r.i16();
        name = r.nullable_string(flex);
    }
    if (version >= 10) {
        topic_id = r.uuid_();
    }
    if (version >= 1) {
        is_internal = r.boolean();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 8) {
        topic_authorized_operations = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_commit_request_partition::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(partition_index);
        w.i64(committed_offset);
    }
    if (version >= 6) {
        w.i32(committed_leader_epoch);
    }
    if (version >= 0) {
        w.nullable_string(committed_metadata, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_commit_request_partition::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        partition_index = r.i32();
        committed_offset = r.i64();
    }
    if (version >= 6) {
        committed_leader_epoch = r.i32();
    }
    if (version >= 0) {
        committed_metadata = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_commit_request_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 9) {
        w.string(name, flex);
    }
    if (version >= 10) {
        w.uuid_(topic_id);
    }
    if (version >= 0) {
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_commit_request_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 9) {
        name = r.string(flex);
    }
    if (version >= 10) {
        topic_id = r.uuid_();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_commit_response_partition::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(partition_index);
        w.i16(error_code);
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_commit_response_partition::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        partition_index = r.i32();
        error_code = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_commit_response_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 9) {
        w.string(name, flex);
    }
    if (version >= 10) {
        w.uuid_(topic_id);
    }
    if (version >= 0) {
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_commit_response_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 9) {
        name = r.string(flex);
    }
    if (version >= 10) {
        topic_id = r.uuid_();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_request_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 7) {
        w.string(name, flex);
        w.array_len(partition_indexes.size(), flex);
        for (const auto& e_ : partition_indexes) {
            w.i32(e_);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_request_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 7) {
        name = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            partition_indexes.clear();
            if (n_ > 0) {
                partition_indexes.resize(static_cast<size_t>(n_));
                for (auto& e_ : partition_indexes) {
                    e_ = r.i32();
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_request_topics::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 8 && version <= 9) {
        w.string(name, flex);
    }
    if (version >= 10) {
        w.uuid_(topic_id);
    }
    if (version >= 8) {
        w.array_len(partition_indexes.size(), flex);
        for (const auto& e_ : partition_indexes) {
            w.i32(e_);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_request_topics::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 8 && version <= 9) {
        name = r.string(flex);
    }
    if (version >= 10) {
        topic_id = r.uuid_();
    }
    if (version >= 8) {
        {
            const int64_t n_ = r.array_len(flex);
            partition_indexes.clear();
            if (n_ > 0) {
                partition_indexes.resize(static_cast<size_t>(n_));
                for (auto& e_ : partition_indexes) {
                    e_ = r.i32();
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_request_group::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 8) {
        w.string(group_id, flex);
    }
    if (version >= 9) {
        w.nullable_string(member_id, flex);
        w.i32(member_epoch);
    }
    if (version >= 8) {
        if (topics.empty() && version >= 8) {
            w.null_array(flex);
        } else {
            w.array_len(topics.size(), flex);
            for (const auto& e_ : topics) {
                e_.encode(w, version, flex);
            }
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_request_group::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 8) {
        group_id = r.string(flex);
    }
    if (version >= 9) {
        member_id = r.nullable_string(flex);
        member_epoch = r.i32();
    }
    if (version >= 8) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_response_partition::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 7) {
        w.i32(partition_index);
        w.i64(committed_offset);
    }
    if (version >= 5 && version <= 7) {
        w.i32(committed_leader_epoch);
    }
    if (version >= 0 && version <= 7) {
        w.nullable_string(metadata, flex);
        w.i16(error_code);
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_response_partition::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 7) {
        partition_index = r.i32();
        committed_offset = r.i64();
    }
    if (version >= 5 && version <= 7) {
        committed_leader_epoch = r.i32();
    }
    if (version >= 0 && version <= 7) {
        metadata = r.nullable_string(flex);
        error_code = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_response_topic::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 7) {
        w.string(name, flex);
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_response_topic::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 7) {
        name = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_response_partitions::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 8) {
        w.i32(partition_index);
        w.i64(committed_offset);
        w.i32(committed_leader_epoch);
        w.nullable_string(metadata, flex);
        w.i16(error_code);
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_response_partitions::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 8) {
        partition_index = r.i32();
        committed_offset = r.i64();
        committed_leader_epoch = r.i32();
        metadata = r.nullable_string(flex);
        error_code = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_response_topics::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 8 && version <= 9) {
        w.string(name, flex);
    }
    if (version >= 10) {
        w.uuid_(topic_id);
    }
    if (version >= 8) {
        w.array_len(partitions.size(), flex);
        for (const auto& e_ : partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_response_topics::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 8 && version <= 9) {
        name = r.string(flex);
    }
    if (version >= 10) {
        topic_id = r.uuid_();
    }
    if (version >= 8) {
        {
            const int64_t n_ = r.array_len(flex);
            partitions.clear();
            if (n_ > 0) {
                partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_response_group::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 8) {
        w.string(group_id, flex);
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            e_.encode(w, version, flex);
        }
        w.i16(error_code);
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_response_group::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 8) {
        group_id = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
        error_code = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_request_partition_produce_data::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(index);
        w.nullable_bytes(records, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void produce_request_partition_produce_data::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        index = r.i32();
        records = r.nullable_bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_request_topic_produce_data::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 12) {
        w.string(name, flex);
    }
    if (version >= 13) {
        w.uuid_(topic_id);
    }
    if (version >= 0) {
        w.array_len(partition_data.size(), flex);
        for (const auto& e_ : partition_data) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void produce_request_topic_produce_data::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 12) {
        name = r.string(flex);
    }
    if (version >= 13) {
        topic_id = r.uuid_();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            partition_data.clear();
            if (n_ > 0) {
                partition_data.resize(static_cast<size_t>(n_));
                for (auto& e_ : partition_data) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_response_batch_index_and_error_message::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 8) {
        w.i32(batch_index);
        w.nullable_string(batch_index_error_message, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void produce_response_batch_index_and_error_message::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 8) {
        batch_index = r.i32();
        batch_index_error_message = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_response_leader_id_and_epoch::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 10) {
        w.i32(leader_id);
        w.i32(leader_epoch);
    }
    if (flex) {
        w.empty_tags();
    }
}

void produce_response_leader_id_and_epoch::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 10) {
        leader_id = r.i32();
        leader_epoch = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_response_partition_produce_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(index);
        w.i16(error_code);
        w.i64(base_offset);
    }
    if (version >= 2) {
        w.i64(log_append_time_ms);
    }
    if (version >= 5) {
        w.i64(log_start_offset);
    }
    if (version >= 8) {
        w.array_len(record_errors.size(), flex);
        for (const auto& e_ : record_errors) {
            e_.encode(w, version, flex);
        }
        w.nullable_string(error_message, flex);
    }
    if (flex) {
        writer t_;
        uint32_t n_ = 0;
        if (version >= 10 && current_leader.has_value()) {
            writer b_;
            current_leader->encode(b_, version, flex);
            t_.uvarint(0);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        w.uvarint(n_);
        w.raw(t_.view());
    }
}

void produce_response_partition_produce_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        index = r.i32();
        error_code = r.i16();
        base_offset = r.i64();
    }
    if (version >= 2) {
        log_append_time_ms = r.i64();
    }
    if (version >= 5) {
        log_start_offset = r.i64();
    }
    if (version >= 8) {
        {
            const int64_t n_ = r.array_len(flex);
            record_errors.clear();
            if (n_ > 0) {
                record_errors.resize(static_cast<size_t>(n_));
                for (auto& e_ : record_errors) {
                    e_.decode(r, version, flex);
                }
            }
        }
        error_message = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            case 0:
                if (version >= 10) {
                    current_leader.emplace();
                    current_leader->decode(r, version, flex);
                }
                break;
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_response_topic_produce_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 12) {
        w.string(name, flex);
    }
    if (version >= 13) {
        w.uuid_(topic_id);
    }
    if (version >= 0) {
        w.array_len(partition_responses.size(), flex);
        for (const auto& e_ : partition_responses) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void produce_response_topic_produce_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 12) {
        name = r.string(flex);
    }
    if (version >= 13) {
        topic_id = r.uuid_();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            partition_responses.clear();
            if (n_ > 0) {
                partition_responses.resize(static_cast<size_t>(n_));
                for (auto& e_ : partition_responses) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_response_node_endpoint::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 10) {
        w.i32(node_id);
        w.string(host, flex);
        w.i32(port);
        w.nullable_string(rack, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void produce_response_node_endpoint::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 10) {
        node_id = r.i32();
        host = r.string(flex);
        port = r.i32();
        rack = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void sync_group_request_assignment::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(member_id, flex);
        w.bytes(assignment, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void sync_group_request_assignment::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        member_id = r.string(flex);
        assignment = r.bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void api_versions_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.string(client_software_name, flex);
        w.string(client_software_version, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void api_versions_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        client_software_name = r.string(flex);
        client_software_version = r.string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void api_versions_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i16(error_code);
        w.array_len(api_keys.size(), flex);
        for (const auto& e_ : api_keys) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 1) {
        w.i32(throttle_time_ms);
    }
    if (flex) {
        writer t_;
        uint32_t n_ = 0;
        if (version >= 3 && !supported_features.empty()) {
            writer b_;
            b_.array_len(supported_features.size(), flex);
            for (const auto& e_ : supported_features) {
                e_.encode(b_, version, flex);
            }
            t_.uvarint(0);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        if (version >= 3 && finalized_features_epoch != -1LL) {
            writer b_;
            b_.i64(finalized_features_epoch);
            t_.uvarint(1);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        if (version >= 3 && !finalized_features.empty()) {
            writer b_;
            b_.array_len(finalized_features.size(), flex);
            for (const auto& e_ : finalized_features) {
                e_.encode(b_, version, flex);
            }
            t_.uvarint(2);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        if (version >= 3 && zk_migration_ready != false) {
            writer b_;
            b_.boolean(zk_migration_ready);
            t_.uvarint(3);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        w.uvarint(n_);
        w.raw(t_.view());
    }
}

void api_versions_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        error_code = r.i16();
        {
            const int64_t n_ = r.array_len(flex);
            api_keys.clear();
            if (n_ > 0) {
                api_keys.resize(static_cast<size_t>(n_));
                for (auto& e_ : api_keys) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 1) {
        throttle_time_ms = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            case 0:
                if (version >= 3) {
                    {
                        const int64_t n_ = r.array_len(flex);
                        supported_features.clear();
                        if (n_ > 0) {
                            supported_features.resize(static_cast<size_t>(n_));
                            for (auto& e_ : supported_features) {
                                e_.decode(r, version, flex);
                            }
                        }
                    }
                }
                break;
            case 1:
                if (version >= 3) {
                    finalized_features_epoch = r.i64();
                }
                break;
            case 2:
                if (version >= 3) {
                    {
                        const int64_t n_ = r.array_len(flex);
                        finalized_features.clear();
                        if (n_ > 0) {
                            finalized_features.resize(static_cast<size_t>(n_));
                            for (auto& e_ : finalized_features) {
                                e_.decode(r, version, flex);
                            }
                        }
                    }
                }
                break;
            case 3:
                if (version >= 3) {
                    zk_migration_ready = r.boolean();
                }
                break;
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void consumer_protocol_assignment::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.array_len(assigned_partitions.size(), flex);
        for (const auto& e_ : assigned_partitions) {
            e_.encode(w, version, flex);
        }
        w.nullable_bytes(user_data, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void consumer_protocol_assignment::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            assigned_partitions.clear();
            if (n_ > 0) {
                assigned_partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : assigned_partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
        user_data = r.nullable_bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void consumer_protocol_subscription::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            w.string(e_, flex);
        }
        w.nullable_bytes(user_data, flex);
    }
    if (version >= 1) {
        w.array_len(owned_partitions.size(), flex);
        for (const auto& e_ : owned_partitions) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 2) {
        w.i32(generation_id);
    }
    if (version >= 3) {
        w.nullable_string(rack_id, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void consumer_protocol_subscription::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_ = r.string(flex);
                }
            }
        }
        user_data = r.nullable_bytes(flex);
    }
    if (version >= 1) {
        {
            const int64_t n_ = r.array_len(flex);
            owned_partitions.clear();
            if (n_ > 0) {
                owned_partitions.resize(static_cast<size_t>(n_));
                for (auto& e_ : owned_partitions) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 2) {
        generation_id = r.i32();
    }
    if (version >= 3) {
        rack_id = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void describe_groups_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.array_len(groups.size(), flex);
        for (const auto& e_ : groups) {
            w.string(e_, flex);
        }
    }
    if (version >= 3) {
        w.boolean(include_authorized_operations);
    }
    if (flex) {
        w.empty_tags();
    }
}

void describe_groups_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            groups.clear();
            if (n_ > 0) {
                groups.resize(static_cast<size_t>(n_));
                for (auto& e_ : groups) {
                    e_ = r.string(flex);
                }
            }
        }
    }
    if (version >= 3) {
        include_authorized_operations = r.boolean();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void describe_groups_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 1) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0) {
        w.array_len(groups.size(), flex);
        for (const auto& e_ : groups) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void describe_groups_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 1) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            groups.clear();
            if (n_ > 0) {
                groups.resize(static_cast<size_t>(n_));
                for (auto& e_ : groups) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 14) {
        w.i32(replica_id);
    }
    if (version >= 0) {
        w.i32(max_wait_ms);
        w.i32(min_bytes);
    }
    if (version >= 3) {
        w.i32(max_bytes);
    }
    if (version >= 4) {
        w.i8(isolation_level);
    }
    if (version >= 7) {
        w.i32(session_id);
        w.i32(session_epoch);
    }
    if (version >= 0) {
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 7) {
        w.array_len(forgotten_topics_data.size(), flex);
        for (const auto& e_ : forgotten_topics_data) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 11) {
        w.string(rack_id, flex);
    }
    if (flex) {
        writer t_;
        uint32_t n_ = 0;
        if (version >= 12 && cluster_id.has_value()) {
            writer b_;
            b_.nullable_string(cluster_id, flex);
            t_.uvarint(0);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        if (version >= 15 && replica_state.has_value()) {
            writer b_;
            replica_state->encode(b_, version, flex);
            t_.uvarint(1);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        w.uvarint(n_);
        w.raw(t_.view());
    }
}

void fetch_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 14) {
        replica_id = r.i32();
    }
    if (version >= 0) {
        max_wait_ms = r.i32();
        min_bytes = r.i32();
    }
    if (version >= 3) {
        max_bytes = r.i32();
    }
    if (version >= 4) {
        isolation_level = r.i8();
    }
    if (version >= 7) {
        session_id = r.i32();
        session_epoch = r.i32();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 7) {
        {
            const int64_t n_ = r.array_len(flex);
            forgotten_topics_data.clear();
            if (n_ > 0) {
                forgotten_topics_data.resize(static_cast<size_t>(n_));
                for (auto& e_ : forgotten_topics_data) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 11) {
        rack_id = r.string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            case 0:
                if (version >= 12) {
                    cluster_id = r.nullable_string(flex);
                }
                break;
            case 1:
                if (version >= 15) {
                    replica_state.emplace();
                    replica_state->decode(r, version, flex);
                }
                break;
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void fetch_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 1) {
        w.i32(throttle_time_ms);
    }
    if (version >= 7) {
        w.i16(error_code);
        w.i32(session_id);
    }
    if (version >= 0) {
        w.array_len(responses.size(), flex);
        for (const auto& e_ : responses) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        writer t_;
        uint32_t n_ = 0;
        if (version >= 16 && !node_endpoints.empty()) {
            writer b_;
            b_.array_len(node_endpoints.size(), flex);
            for (const auto& e_ : node_endpoints) {
                e_.encode(b_, version, flex);
            }
            t_.uvarint(0);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        w.uvarint(n_);
        w.raw(t_.view());
    }
}

void fetch_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 1) {
        throttle_time_ms = r.i32();
    }
    if (version >= 7) {
        error_code = r.i16();
        session_id = r.i32();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            responses.clear();
            if (n_ > 0) {
                responses.resize(static_cast<size_t>(n_));
                for (auto& e_ : responses) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            case 0:
                if (version >= 16) {
                    {
                        const int64_t n_ = r.array_len(flex);
                        node_endpoints.clear();
                        if (n_ > 0) {
                            node_endpoints.resize(static_cast<size_t>(n_));
                            for (auto& e_ : node_endpoints) {
                                e_.decode(r, version, flex);
                            }
                        }
                    }
                }
                break;
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void find_coordinator_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 3) {
        w.string(key, flex);
    }
    if (version >= 1) {
        w.i8(key_type);
    }
    if (version >= 4) {
        w.array_len(coordinator_keys.size(), flex);
        for (const auto& e_ : coordinator_keys) {
            w.string(e_, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void find_coordinator_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 3) {
        key = r.string(flex);
    }
    if (version >= 1) {
        key_type = r.i8();
    }
    if (version >= 4) {
        {
            const int64_t n_ = r.array_len(flex);
            coordinator_keys.clear();
            if (n_ > 0) {
                coordinator_keys.resize(static_cast<size_t>(n_));
                for (auto& e_ : coordinator_keys) {
                    e_ = r.string(flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void find_coordinator_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 1) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0 && version <= 3) {
        w.i16(error_code);
    }
    if (version >= 1 && version <= 3) {
        w.nullable_string(error_message, flex);
    }
    if (version >= 0 && version <= 3) {
        w.i32(node_id);
        w.string(host, flex);
        w.i32(port);
    }
    if (version >= 4) {
        w.array_len(coordinators.size(), flex);
        for (const auto& e_ : coordinators) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void find_coordinator_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 1) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0 && version <= 3) {
        error_code = r.i16();
    }
    if (version >= 1 && version <= 3) {
        error_message = r.nullable_string(flex);
    }
    if (version >= 0 && version <= 3) {
        node_id = r.i32();
        host = r.string(flex);
        port = r.i32();
    }
    if (version >= 4) {
        {
            const int64_t n_ = r.array_len(flex);
            coordinators.clear();
            if (n_ > 0) {
                coordinators.resize(static_cast<size_t>(n_));
                for (auto& e_ : coordinators) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void heartbeat_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(group_id, flex);
        w.i32(generation_id);
        w.string(member_id, flex);
    }
    if (version >= 3) {
        w.nullable_string(group_instance_id, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void heartbeat_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        group_id = r.string(flex);
        generation_id = r.i32();
        member_id = r.string(flex);
    }
    if (version >= 3) {
        group_instance_id = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void heartbeat_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 1) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0) {
        w.i16(error_code);
    }
    if (flex) {
        w.empty_tags();
    }
}

void heartbeat_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 1) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0) {
        error_code = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void join_group_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(group_id, flex);
        w.i32(session_timeout_ms);
    }
    if (version >= 1) {
        w.i32(rebalance_timeout_ms);
    }
    if (version >= 0) {
        w.string(member_id, flex);
    }
    if (version >= 5) {
        w.nullable_string(group_instance_id, flex);
    }
    if (version >= 0) {
        w.string(protocol_type, flex);
        w.array_len(protocols.size(), flex);
        for (const auto& e_ : protocols) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 8) {
        w.nullable_string(reason, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void join_group_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        group_id = r.string(flex);
        session_timeout_ms = r.i32();
    }
    if (version >= 1) {
        rebalance_timeout_ms = r.i32();
    }
    if (version >= 0) {
        member_id = r.string(flex);
    }
    if (version >= 5) {
        group_instance_id = r.nullable_string(flex);
    }
    if (version >= 0) {
        protocol_type = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            protocols.clear();
            if (n_ > 0) {
                protocols.resize(static_cast<size_t>(n_));
                for (auto& e_ : protocols) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 8) {
        reason = r.nullable_string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void join_group_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 2) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0) {
        w.i16(error_code);
        w.i32(generation_id);
    }
    if (version >= 7) {
        w.nullable_string(protocol_type, flex);
    }
    if (version >= 0) {
        w.nullable_string(protocol_name, flex);
        w.string(leader, flex);
    }
    if (version >= 9) {
        w.boolean(skip_assignment);
    }
    if (version >= 0) {
        w.string(member_id, flex);
        w.array_len(members.size(), flex);
        for (const auto& e_ : members) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void join_group_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 2) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0) {
        error_code = r.i16();
        generation_id = r.i32();
    }
    if (version >= 7) {
        protocol_type = r.nullable_string(flex);
    }
    if (version >= 0) {
        protocol_name = r.nullable_string(flex);
        leader = r.string(flex);
    }
    if (version >= 9) {
        skip_assignment = r.boolean();
    }
    if (version >= 0) {
        member_id = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            members.clear();
            if (n_ > 0) {
                members.resize(static_cast<size_t>(n_));
                for (auto& e_ : members) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void leave_group_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(group_id, flex);
    }
    if (version >= 0 && version <= 2) {
        w.string(member_id, flex);
    }
    if (version >= 3) {
        w.array_len(members.size(), flex);
        for (const auto& e_ : members) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void leave_group_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        group_id = r.string(flex);
    }
    if (version >= 0 && version <= 2) {
        member_id = r.string(flex);
    }
    if (version >= 3) {
        {
            const int64_t n_ = r.array_len(flex);
            members.clear();
            if (n_ > 0) {
                members.resize(static_cast<size_t>(n_));
                for (auto& e_ : members) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void leave_group_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 1) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0) {
        w.i16(error_code);
    }
    if (version >= 3) {
        w.array_len(members.size(), flex);
        for (const auto& e_ : members) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void leave_group_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 1) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0) {
        error_code = r.i16();
    }
    if (version >= 3) {
        {
            const int64_t n_ = r.array_len(flex);
            members.clear();
            if (n_ > 0) {
                members.resize(static_cast<size_t>(n_));
                for (auto& e_ : members) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void list_offsets_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i32(replica_id);
    }
    if (version >= 2) {
        w.i8(isolation_level);
    }
    if (version >= 0) {
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 10) {
        w.i32(timeout_ms);
    }
    if (flex) {
        w.empty_tags();
    }
}

void list_offsets_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        replica_id = r.i32();
    }
    if (version >= 2) {
        isolation_level = r.i8();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 10) {
        timeout_ms = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void list_offsets_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 2) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0) {
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void list_offsets_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 2) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void metadata_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        if (topics.empty() && version >= 1) {
            w.null_array(flex);
        } else {
            w.array_len(topics.size(), flex);
            for (const auto& e_ : topics) {
                e_.encode(w, version, flex);
            }
        }
    }
    if (version >= 4) {
        w.boolean(allow_auto_topic_creation);
    }
    if (version >= 8 && version <= 10) {
        w.boolean(include_cluster_authorized_operations);
    }
    if (version >= 8) {
        w.boolean(include_topic_authorized_operations);
    }
    if (flex) {
        w.empty_tags();
    }
}

void metadata_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 4) {
        allow_auto_topic_creation = r.boolean();
    }
    if (version >= 8 && version <= 10) {
        include_cluster_authorized_operations = r.boolean();
    }
    if (version >= 8) {
        include_topic_authorized_operations = r.boolean();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void metadata_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0) {
        w.array_len(brokers.size(), flex);
        for (const auto& e_ : brokers) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 2) {
        w.nullable_string(cluster_id, flex);
    }
    if (version >= 1) {
        w.i32(controller_id);
    }
    if (version >= 0) {
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 8 && version <= 10) {
        w.i32(cluster_authorized_operations);
    }
    if (version >= 13) {
        w.i16(error_code);
    }
    if (flex) {
        w.empty_tags();
    }
}

void metadata_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            brokers.clear();
            if (n_ > 0) {
                brokers.resize(static_cast<size_t>(n_));
                for (auto& e_ : brokers) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 2) {
        cluster_id = r.nullable_string(flex);
    }
    if (version >= 1) {
        controller_id = r.i32();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 8 && version <= 10) {
        cluster_authorized_operations = r.i32();
    }
    if (version >= 13) {
        error_code = r.i16();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_commit_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(group_id, flex);
    }
    if (version >= 1) {
        w.i32(generation_id_or_member_epoch);
        w.string(member_id, flex);
    }
    if (version >= 7) {
        w.nullable_string(group_instance_id, flex);
    }
    if (version >= 2 && version <= 4) {
        w.i64(retention_time_ms);
    }
    if (version >= 0) {
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_commit_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        group_id = r.string(flex);
    }
    if (version >= 1) {
        generation_id_or_member_epoch = r.i32();
        member_id = r.string(flex);
    }
    if (version >= 7) {
        group_instance_id = r.nullable_string(flex);
    }
    if (version >= 2 && version <= 4) {
        retention_time_ms = r.i64();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_commit_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0) {
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_commit_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0 && version <= 7) {
        w.string(group_id, flex);
        if (topics.empty() && version >= 2 && version <= 7) {
            w.null_array(flex);
        } else {
            w.array_len(topics.size(), flex);
            for (const auto& e_ : topics) {
                e_.encode(w, version, flex);
            }
        }
    }
    if (version >= 8) {
        w.array_len(groups.size(), flex);
        for (const auto& e_ : groups) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 7) {
        w.boolean(require_stable);
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0 && version <= 7) {
        group_id = r.string(flex);
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 8) {
        {
            const int64_t n_ = r.array_len(flex);
            groups.clear();
            if (n_ > 0) {
                groups.resize(static_cast<size_t>(n_));
                for (auto& e_ : groups) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 7) {
        require_stable = r.boolean();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void offset_fetch_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0 && version <= 7) {
        w.array_len(topics.size(), flex);
        for (const auto& e_ : topics) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 2 && version <= 7) {
        w.i16(error_code);
    }
    if (version >= 8) {
        w.array_len(groups.size(), flex);
        for (const auto& e_ : groups) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void offset_fetch_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0 && version <= 7) {
        {
            const int64_t n_ = r.array_len(flex);
            topics.clear();
            if (n_ > 0) {
                topics.resize(static_cast<size_t>(n_));
                for (auto& e_ : topics) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 2 && version <= 7) {
        error_code = r.i16();
    }
    if (version >= 8) {
        {
            const int64_t n_ = r.array_len(flex);
            groups.clear();
            if (n_ > 0) {
                groups.resize(static_cast<size_t>(n_));
                for (auto& e_ : groups) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 3) {
        w.nullable_string(transactional_id, flex);
    }
    if (version >= 0) {
        w.i16(acks);
        w.i32(timeout_ms);
        w.array_len(topic_data.size(), flex);
        for (const auto& e_ : topic_data) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void produce_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 3) {
        transactional_id = r.nullable_string(flex);
    }
    if (version >= 0) {
        acks = r.i16();
        timeout_ms = r.i32();
        {
            const int64_t n_ = r.array_len(flex);
            topic_data.clear();
            if (n_ > 0) {
                topic_data.resize(static_cast<size_t>(n_));
                for (auto& e_ : topic_data) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void produce_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.array_len(responses.size(), flex);
        for (const auto& e_ : responses) {
            e_.encode(w, version, flex);
        }
    }
    if (version >= 1) {
        w.i32(throttle_time_ms);
    }
    if (flex) {
        writer t_;
        uint32_t n_ = 0;
        if (version >= 10 && !node_endpoints.empty()) {
            writer b_;
            b_.array_len(node_endpoints.size(), flex);
            for (const auto& e_ : node_endpoints) {
                e_.encode(b_, version, flex);
            }
            t_.uvarint(0);
            t_.uvarint(static_cast<uint32_t>(b_.size()));
            t_.raw(b_.view());
            ++n_;
        }
        w.uvarint(n_);
        w.raw(t_.view());
    }
}

void produce_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            responses.clear();
            if (n_ > 0) {
                responses.resize(static_cast<size_t>(n_));
                for (auto& e_ : responses) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (version >= 1) {
        throttle_time_ms = r.i32();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            case 0:
                if (version >= 10) {
                    {
                        const int64_t n_ = r.array_len(flex);
                        node_endpoints.clear();
                        if (n_ > 0) {
                            node_endpoints.resize(static_cast<size_t>(n_));
                            for (auto& e_ : node_endpoints) {
                                e_.decode(r, version, flex);
                            }
                        }
                    }
                }
                break;
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void sasl_authenticate_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.bytes(auth_bytes, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void sasl_authenticate_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        auth_bytes = r.bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void sasl_authenticate_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i16(error_code);
        w.nullable_string(error_message, flex);
        w.bytes(auth_bytes, flex);
    }
    if (version >= 1) {
        w.i64(session_lifetime_ms);
    }
    if (flex) {
        w.empty_tags();
    }
}

void sasl_authenticate_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        error_code = r.i16();
        error_message = r.nullable_string(flex);
        auth_bytes = r.bytes(flex);
    }
    if (version >= 1) {
        session_lifetime_ms = r.i64();
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void sasl_handshake_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(mechanism, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void sasl_handshake_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        mechanism = r.string(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void sasl_handshake_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.i16(error_code);
        w.array_len(mechanisms.size(), flex);
        for (const auto& e_ : mechanisms) {
            w.string(e_, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void sasl_handshake_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        error_code = r.i16();
        {
            const int64_t n_ = r.array_len(flex);
            mechanisms.clear();
            if (n_ > 0) {
                mechanisms.resize(static_cast<size_t>(n_));
                for (auto& e_ : mechanisms) {
                    e_ = r.string(flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void sync_group_request::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 0) {
        w.string(group_id, flex);
        w.i32(generation_id);
        w.string(member_id, flex);
    }
    if (version >= 3) {
        w.nullable_string(group_instance_id, flex);
    }
    if (version >= 5) {
        w.nullable_string(protocol_type, flex);
        w.nullable_string(protocol_name, flex);
    }
    if (version >= 0) {
        w.array_len(assignments.size(), flex);
        for (const auto& e_ : assignments) {
            e_.encode(w, version, flex);
        }
    }
    if (flex) {
        w.empty_tags();
    }
}

void sync_group_request::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 0) {
        group_id = r.string(flex);
        generation_id = r.i32();
        member_id = r.string(flex);
    }
    if (version >= 3) {
        group_instance_id = r.nullable_string(flex);
    }
    if (version >= 5) {
        protocol_type = r.nullable_string(flex);
        protocol_name = r.nullable_string(flex);
    }
    if (version >= 0) {
        {
            const int64_t n_ = r.array_len(flex);
            assignments.clear();
            if (n_ > 0) {
                assignments.resize(static_cast<size_t>(n_));
                for (auto& e_ : assignments) {
                    e_.decode(r, version, flex);
                }
            }
        }
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

void sync_group_response::encode(writer& w, int16_t version, bool flex) const {
    (void)w; (void)version; (void)flex;
    if (version >= 1) {
        w.i32(throttle_time_ms);
    }
    if (version >= 0) {
        w.i16(error_code);
    }
    if (version >= 5) {
        w.nullable_string(protocol_type, flex);
        w.nullable_string(protocol_name, flex);
    }
    if (version >= 0) {
        w.bytes(assignment, flex);
    }
    if (flex) {
        w.empty_tags();
    }
}

void sync_group_response::decode(reader& r, int16_t version, bool flex) {
    (void)r; (void)version; (void)flex;
    if (version >= 1) {
        throttle_time_ms = r.i32();
    }
    if (version >= 0) {
        error_code = r.i16();
    }
    if (version >= 5) {
        protocol_type = r.nullable_string(flex);
        protocol_name = r.nullable_string(flex);
    }
    if (version >= 0) {
        assignment = r.bytes(flex);
    }
    if (flex) {
        const uint32_t nt_ = r.uvarint();
        for (uint32_t i_ = 0; i_ < nt_; ++i_) {
            const uint32_t tag_ = r.uvarint();
            const uint32_t sz_ = r.uvarint();
            const size_t end_ = r.pos() + sz_;
            switch (tag_) {
            default: break;    // a tag from a newer broker
            }
            r.seek(end_);      // authoritative, even if we skipped it
        }
    }
}

} // namespace sf::kafka
